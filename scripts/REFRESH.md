# HIT PICK data refresh

This is the existing Site identified ONLY by `.openai/hosting.json`. Never create a replacement Site. The user authorized daily crawling and publishing of KBO statistics to this same private Site.

1. Read the Sites building/hosting skills, get this existing Site, and recover its source checkout from the Sites repository if necessary. Fetch current remote source before editing; do not overwrite newer user changes with an old checkout.
2. Run `python scripts/crawl_kbo.py` in the Site checkout. This uses Python standard library only. It GETs the public official batting table, then submits the page's own pagination controls with its form state and a session cookie jar. Leaderboard pagination is sequential; player-detail reads use at most three concurrent requests, each delayed one second, with a 45-second timeout. It validates the selected season, headers, columns, H/AB versus AVG, page advancement, contiguous ranks and all players' recent 10-game totals. If any player detail fails, do not replace the complete prior snapshot. Data replacement is atomic only after all pages succeed.
3. If crawling fails, preserve the last successful snapshot and deployment. Report the actual failure. Do not invent data, replace it with samples, relabel an old snapshot as fresh, or bypass access restrictions.
4. Inspect `dist/kbo-data.json` for the current season, successful timestamp and complete pages. Normal current scope is the default leaderboard (48 rows / 2 pages at initial validation); counts can change. This is not all registered players. `record_cutoff: null` means KBO does not provide an exact last-included-game cutoff on this table. `fetched_at` is collection time only.
5. Preserve UI and prediction logic; change the data snapshot only for routine refresh. The UI assumes four at-bats for every player. Recent 10-game performance and opponent-team splits are collected from each official player page using its player ID. Sum game-row H and AB and cross-check the KBO totals. Preserve game_log, games, date range, opponent split totals and source URLs. The monthly official schedule feed is collected too; it determines which teams have a scheduled game for a chosen date. Completed and cancelled games are excluded from recommendations. Lineups and opposing starting pitchers are uncollected, so do not mark starters confirmed. Never set a player as confirmed or turn missing records into zero-hit statistics.
6. The Site is static. Completing a crawl does NOT publish. Follow Sites hosting: commit the refreshed source, push with a fresh per-command credential, get full HEAD SHA after successful push, package that exact revision using the skill helper, save the version, publish within the user's authorized existing audience, and check terminal deployment status. Do not change sharing. Keep tokens out of source and logs.
7. Report completion only after deployment succeeds. Include collection time, player count, and the same Site URL. On failure report that the previous deployed data remains. No email/Slack actions are authorized.

Schedule owner: ChatGPT Automation; Asia/Seoul 00:00 daily. The automation starts collection at midnight; publication follows after processing. This is not a native server cron or an instant midnight update. The UI marks a successful snapshot older than 26 hours as delayed.
